server {
    server_name pihole.{{ hostname }};
    listen 443 ssl;
    include /etc/nginx/custom_common.conf;

    location / {
        proxy_pass http://localhost:{{ ports.pihole }}/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
