server {
    server_name portainer.{{ hostname }};
    listen 443 ssl;
    include /etc/nginx/custom_common.conf;

    location / {
        proxy_pass https://localhost:{{ ports.portainer_https }}/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
